import { Mic } from 'lucide-react';
import type React from 'react';

const LiveScribeLogo = (props: React.SVGProps<SVGSVGElement>) => {
  return <Mic {...props} />;
};

export default LiveScribeLogo;
